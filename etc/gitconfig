[user]
	email = rj@rybar.tech
	name = RJ Rybarczyk

[core]
  editor = vim
	pager = less -RXF

[alias]
    ci = commit

[color]
    ui = always
    diff = always
    status = always
    grep = always
    showbranch = always

[color "status"]
    added = black magenta 
    changed = black yellow
    untracked = cyan bold

[color "branch"]
    remote  = green white
    upstream = yellow white
    local = blue white

[color "grep"]
    context = blue ul
    lineNumber = magenta

[color "push"]
    error = red bold
  
[pull]
  rebase = true

# Alias for ignoring pre-push git hooks: `git push --no-verify <remote> <branch>`
[alias]
  push-nv = "!git push --no-verify"
